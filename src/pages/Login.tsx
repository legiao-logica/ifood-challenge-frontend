import React from 'react';
import LoginComponent from '../components/login/Login';

const LoginPage = () => {
    return (
        <div>
            <LoginComponent />
        </div>
    );
};

export default LoginPage;